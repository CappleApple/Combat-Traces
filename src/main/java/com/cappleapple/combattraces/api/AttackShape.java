package com.cappleapple.combattraces.api;

/**
 * Optional attack-volume metadata. FORWARD is a hint, not proof of a thrust (a slam also uses it).
 */
public enum AttackShape {
  UNKNOWN,
  SWEEP,
  FORWARD
}
